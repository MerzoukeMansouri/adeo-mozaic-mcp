[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build:nest"]

[start]
cmd = "node dist/main.js"

[variables]
NODE_ENV = "production"
DATABASE_PATH = "/app/data/mozaic.db"
PORT = "3000"